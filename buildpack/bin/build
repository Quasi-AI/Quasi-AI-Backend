#!/bin/bash
set -e

echo "Building Node.js app..."
cd "$1" || exit 1

# Install dependencies
npm install --production

# Set the start command for Cloud Run
echo '{"start": "node server.js"}' > start.json

echo "Build complete."
